[build]
  base    = "frontend"
  command = "npm ci && npm run build -- --configuration production"
  publish = "dist/flowlyrent/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
