export const environment = {
  production: true,
  apiUrl: 'https://flowlyrent-api.up.railway.app',   // ← remplacer par ton URL Railway après déploiement
  stripePublishableKey: 'pk_live_placeholder',        // ← clé Stripe live
  wsUrl: 'wss://flowlyrent-api.up.railway.app/ws'    // ← même domaine Railway
};
